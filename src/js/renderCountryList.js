const renderCountryList = (result) => {
  const dataList = document.querySelector('#country-suggestions');
  //TODO aoi request to get the list of countries
  // const countries = await fetchCountries();

  // dataList.innerHTML = countries.map(country => `<option value="${country}"></option>`).join('');
};

export default renderCountryList;